{
  "name": "sechs_nimmt",
  "version": "1.0.0",
  "description": "A simulation of a game called 6nimmt to see which strategy is the best",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "push": "git add . && git commit -m \"Automated push\" && git push"
  },
  "keywords": [],
  "type": "module",
  "dependencies": {
    "inquirer": "^14.0.2"
  }
}
